<%= form_with(model: kid, local: true) do |form| %>
  <% if kid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kid.errors.count, "error") %> prohibited this kid from being saved:</h2>

      <ul>
      <% kid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
  Candies : 
   <%= form.collection_check_boxes :candy_ids, Candy.all, :id, :name do |candy|%>
        <label>
          <%= candy.check_box %>
          <%= candy.text%>
        </label>
        <!-- <br/> -->
    <%end%>                            
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
