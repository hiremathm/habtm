<%= form_with(model: candy, local: true) do |form| %>
  <% if candy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(candy.errors.count, "error") %> prohibited this candy from being saved:</h2>

      <ul>
      <% candy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    Kids : 
    <%#= form.collection_check_boxes(:candy, :kid_ids, @kids, :id, :name) %>        
    <%= form.collection_check_boxes :kid_ids, Kid.all, :id, :name do |kid|%>
        <label>
          <%= kid.check_box %>
          <%= kid.text%>
        </label>
        <!-- <br/> -->
    <%end%>             
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
